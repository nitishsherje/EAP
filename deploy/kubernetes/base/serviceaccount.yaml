apiVersion: v1
kind: ServiceAccount
metadata:
  name: eap
  labels:
    app.kubernetes.io/name: eap
  # In EKS, associate an IAM role for S3 / other AWS access via IRSA:
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<account>:role/eap-runtime
